@model IEnumerable<LoanManagementSystem.Models.ApplicationApproval>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LoanApplication)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApplicationStatus)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.LoanApplication.ApplicationHolderName)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApplicationStatus)
            </td>
           
            <td>
                <a asp-action="Edit" asp-route-id="@item.ApprovalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ApprovalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ApprovalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
